import React from 'react';
import { Navbar } from '../../widgets/navbar';
import { ListCard } from '../../widgets';

const Home = () => {
  return (
    <div>
      <Navbar/>
      <ListCard/>
    </div>
  );
};

export default Home;